{
  "scripts": {
    "publish": "cargo publish",
    "lint": "cargo fmt --all",
    "dev": "cargo run",
    "clean": "cargo clean",
    "remove": "cargo remove",
    "build": "cargo build",
    "install": "cargo install --path .",
    "add": "cargo add"
  }
}